# Offline fixtures

**Reviewed 2026-09-05.** Synthetic engineering fixtures only. This review does not regenerate fixtures or turn them into research evidence.

Fixtures are synthetic and exist only to test invariants. They are not citable research data and must never be reported as a `coincidence_v0` result.

`cutoff_versions.json` proves that a later visible revision cannot affect an earlier cutoff. Expected values are declared by the cutoff rule, not generated by production feature code.

Connector tests build Wikipedia, ALFRED, GDELT, MOEX, wiki-edit, and Sentinel-1 Statistical API HTTP bodies in memory. They are not citable harvests.

ICEWS: set `ICEWS_EVENTS_PATH` to `data/raw/icews/dataverse_files.zip` (or a directory of yearly `.tab` files). Do not commit the zip. FIRMS: free MAP_KEY from https://firms.modaps.eosdis.nasa.gov/api/; default sensor `VIIRS_NOAA20_SP`. Sentinel-1 is opt-in (Copernicus OAuth) and disabled on the default panel.

