# Offline fixtures

Fixtures are synthetic and exist only to test invariants. They are not citable research data and must never be reported as a `coincidence_v0` result.

`cutoff_versions.json` proves that a later visible revision cannot affect an earlier cutoff. Expected values are declared by the cutoff rule, not generated by production feature code.

